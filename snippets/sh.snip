# #!/bin/bash
snippet binsh
	#!/usr/bin/env bash

snippet if
	if [[ ${1:condition} ]]; then
		${2:#statements}
	fi
snippet ife
	if [[ ${1:condition} ]]; then
		${2:#statements}
	else
		${3:#statements}
	fi
snippet elif
	elif [[ ${1:condition} ]]; then
		${2:#statements}
